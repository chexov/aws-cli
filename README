# Check the updates on
# http://github.com/chexov/aws-cli

Description
===========
This scripts may help you to install all the AWS tools in one shot
in your home dir.
And will run them from that place and not from /usr/local.



Installation
============
git clone http://github.com/chexov/aws-cli
cd aws-cli
./update-tools.sh

To apply the environment:
. ./profile

You may want add aws-cli/profile file to your ~/.profile

Have a happy day.

